<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.etr.mapper.UserMapper">
    <resultMap id="user" type="com.etr.model.User">
        <result column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="open_id" property="viewMiniId"/>

        <result column="create_time" property="viewCreateTime"/>
        <result column="update_time" property="viewUpdateTime"/>

    </resultMap>



    <select id="findUserById" parameterType="Integer" resultMap="user">

        SELECT * FROM user  WHERE  id  = #{projectId}
    </select>

    <select id="getUserInfobyOpenID" parameterType="String" resultMap="user">
        SELECT * FROM user  WHERE  id  = #{openId}
    </select>

    <insert id="addUser" parameterType="com.etr.model.User" useGeneratedKeys="true" keyProperty="id">
          INSERT INTO view
          (openId)
          VALUES
          (#{openId})
    </insert>


</mapper>